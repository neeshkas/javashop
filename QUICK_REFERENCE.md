# 🎯 ШПАРГАЛКА ДЛЯ ПРЕЗЕНТАЦИИ - REQUIREMENT 6

## ⚡ КЛЮЧЕВЫЕ ЦИФРЫ ДЛЯ ДЕМОНСТРАЦИИ

### Laptop (450,000 KZT, вес 1.8 кг, доставка ~180 KZT)
**qty=1:**
- Без скидок: 450,000 KZT
- Со скидкой 10%: **405,180 KZT** ← ЛУЧШАЯ (экономия 45K)
- BOGO: 450,180 KZT (нет пары)

**qty=2:**
- Без скидок: 900,000 KZT
- Со скидкой 10%: 810,180 KZT
- BOGO: **675,180 KZT** ← ЛУЧШАЯ (экономия 225K!)

### E-Book (1,500 KZT, нет доставки)
**qty=1:**
- Без скидок: 1,500 KZT
- Со скидкой 10%: **1,350 KZT** ← ЛУЧШАЯ
- BOGO: 1,500 KZT (ИГНОРИРУЕТСЯ!)

**qty=2:**
- Без скидок: 3,000 KZT
- Со скидкой 10%: **2,700 KZT** ← ЛУЧШАЯ
- BOGO: 3,000 KZT (ИГНОРИРУЕТСЯ!)

---

## 📌 ТРИ ТИПА ПОЛИМОРФИЗМА

### 1️⃣ Interface-based (Интерфейсы)
```
PricePolicy (интерфейс)
    ↓
PercentageOff / FixedOff / BogoHalf (реализации)
```
**Суть:** Product работает с интерфейсом, не зная конкретных реализаций

### 2️⃣ Compile-time (Перегрузка методов)
```java
finalPrice()                              // 0 параметров
finalPrice(int qty)                       // 1 параметр
finalPrice(int qty, PricePolicy policy)   // 2 параметра
finalPrice(int qty, List<PricePolicy>)    // 2 параметра, другой тип
```
**Суть:** Компилятор выбирает метод по сигнатуре

### 3️⃣ Runtime (Переопределение методов)
```java
Product.finalPrice()           → базовая цена
PhysicalProduct.finalPrice()   → + доставка
DigitalProduct.finalPrice()    → игнорирует BOGO
```
**Суть:** JVM выбирает метод по фактическому типу объекта

---

## 💡 ФОРМУЛЫ ПОЛИТИК

### PercentageOff (процентная скидка)
```
Цена = price × (1 - percent/100) × qty
Пример: 1000 × (1 - 10/100) × 2 = 1800 KZT
```

### FixedOff (фиксированная скидка)
```
Цена = max(0, price - amount) × qty
Пример: max(0, 1000 - 50) × 2 = 1900 KZT
```

### BogoHalf (купи 1, получи 2-й за полцены)
```
pairs = qty ÷ 2
singles = qty % 2
Цена = pairs × (price × 1.5) + singles × price
Пример qty=2: 1 × (1000 × 1.5) + 0 × 1000 = 1500 KZT
```

---

## 🎤 КЛЮЧЕВЫЕ ФРАЗЫ

**При показе интерфейса:**
> "PricePolicy - это контракт, который НЕ мутирует товар, а ВЫЧИСЛЯЕТ новую цену"

**При показе перегрузки:**
> "Четыре метода с одним именем - компилятор выбирает нужный по сигнатуре"

**При показе переопределения:**
> "PhysicalProduct добавляет доставку, DigitalProduct игнорирует BOGO - одна строка кода, разное поведение"

**При демонстрации BOGO для Laptop qty=2:**
> "Смотрите - 675 тысяч вместо 900! Экономия 225 тысяч - почти четверть миллиона!"

**При демонстрации BOGO для E-Book:**
> "А вот для цифрового товара BOGO игнорируется - вернулась базовая цена 3000"

**При выводе:**
> "Это Strategy Pattern + полиморфизм - легко добавлять новые политики без изменения существующего кода. Open/Closed Principle в действии!"

---

## 🔥 ФИШКИ ДЛЯ ВПЕЧАТЛЕНИЯ

1. **Проблема старого подхода:**
   - "applyDiscount() мутировал товар - цена менялась навсегда"
   - "Повторный вызов применял скидку к уже сниженной цене"
   - "Нельзя было сравнить варианты или откатить изменения"

2. **Преимущества нового:**
   - "Товар не меняется - можем пересчитывать цену сколько угодно"
   - "Легко добавить новую политику - один класс, остальное не трогаем"
   - "Каждый тип товара реагирует по-своему - это и есть полиморфизм"

3. **Реальный мир:**
   - "Amazon использует похожую систему для dynamic pricing"
   - "Kaspi.kz - кредиты, рассрочки, промокоды - все это политики"

---

## 🎬 ПОРЯДОК ДЕМОНСТРАЦИИ (10 минут)

**1. Введение (1 мин)**
- Что было до: Product + PhysicalProduct + DigitalProduct
- Проблема: applyDiscount() мутирует товар
- Задача: гибкая система с тремя типами полиморфизма

**2. Показ кода (5 мин)**
- PricePolicy.java (интерфейс) - 30 сек
- BogoHalf.java (самая интересная реализация) - 1 мин
- Product.java (4 метода finalPrice) - 1 мин
- PhysicalProduct.java (+ доставка) - 1 мин
- DigitalProduct.java (игнорирует BOGO) - 1 мин
- ShopDemo5.java (полиморфные списки) - 30 сек

**3. Запуск программы (3 мин)**
```bash
javac -d out src/category/*.java src/product/*.java src/product/pricing/*.java
java -cp out product.ShopDemo5
```
- Объяснить вывод для Laptop qty=1 и qty=2
- Объяснить вывод для E-Book
- Подчеркнуть: доставка для Physical, игнорирование BOGO для Digital

**4. Выводы (1 мин)**
- ✅ Interface-based polymorphism
- ✅ Method Overloading (compile-time)
- ✅ Method Overriding (runtime)
- ✅ Strategy Pattern
- ✅ Open/Closed Principle

---

## ❓ ТОП-3 ВОПРОСА И ОТВЕТЫ

### Q: Почему интерфейс, а не абстрактный класс?
**A:** "Нет общего состояния между политиками, только контракт. Интерфейс идеален для этого. Плюс default метод applicableTo()"

### Q: Почему apply() не изменяет цену?
**A:** "Immutability - можем пересчитывать, сравнивать варианты, не боясь изменить исходные данные. В БД цена остается неизменной."

### Q: Как выбирается лучшая политика?
**A:** "Перебираем все политики, вызываем apply() для каждой, сохраняем минимум. Это выгоднее для покупателя."

---

## 📋 ЧЕКЛИСТ ПЕРЕД ПРЕЗЕНТАЦИЕЙ

- [ ] Проект скомпилирован: `javac -d out src/category/*.java src/product/*.java src/product/pricing/*.java`
- [ ] Программа запускается: `java -cp out product.ShopDemo5`
- [ ] Открыты файлы в IDE:
  - [ ] PricePolicy.java
  - [ ] BogoHalf.java
  - [ ] Product.java (строки 159-192)
  - [ ] PhysicalProduct.java (строки 77-94)
  - [ ] DigitalProduct.java (строки 64-70)
- [ ] Терминал готов
- [ ] Эта шпаргалка открыта

---

## 🎯 ГЛАВНОЕ

**Requirement 6 = ТРИ типа полиморфизма:**
1. Интерфейсы (PricePolicy)
2. Перегрузка (4 метода finalPrice)
3. Переопределение (Physical + доставка, Digital - BOGO)

**Результат:** Гибкая, расширяемая система ценообразования по принципу Strategy Pattern.

**УДАЧИ! 🚀**
