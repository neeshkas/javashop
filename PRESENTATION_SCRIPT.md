# ПРЕЗЕНТАЦИЯ REQUIREMENT 6: ПОЛИМОРФИЗМ В СИСТЕМЕ E-COMMERCE

## 📋 ПЛАН ПРЕЗЕНТАЦИИ (10-15 минут)

1. Введение и контекст проекта (2 мин)
2. Архитектура и новые компоненты (3 мин)
3. Демонстрация полиморфизма в коде (5 мин)
4. Live-демонстрация работы (3 мин)
5. Ключевые выводы (2 мин)

---

## 🎤 ТЕКСТ ПРЕЗЕНТАЦИИ

### ЧАСТЬ 1: ВВЕДЕНИЕ (2 минуты)

**[Слайд/Открытие проекта]**

"Добрый день! Сегодня я представлю Requirement 6 моего проекта - систему интернет-магазина, которая демонстрирует три типа полиморфизма в Java.

**Контекст проекта:**
Мой проект - это система управления товарами для e-commerce. До Requirement 6 я реализовал:
- Базовый класс Product с инкапсуляцией и валидацией данных
- Два подкласса: PhysicalProduct (физические товары) и DigitalProduct (цифровые товары)
- Статические методы и фабрики для создания объектов

**Проблема старого подхода:**
У меня был метод `applyDiscount()`, который изменял цену товара напрямую. Это создавало три проблемы:
1. Товар мутировался - цена изменялась навсегда
2. Был только один тип скидки - процентная
3. Нельзя было сравнить разные варианты цен

**Задача Requirement 6:**
Создать гибкую систему ценообразования, которая демонстрирует:
- Interface-based polymorphism - через интерфейсы
- Compile-time polymorphism - через перегрузку методов
- Runtime polymorphism - через переопределение методов"

---

### ЧАСТЬ 2: АРХИТЕКТУРА (3 минуты)

**[Показать структуру файлов]**

"Давайте посмотрим на архитектуру. Requirement 6 добавил новый пакет `product.pricing` с системой ценовых политик.

**Структура проекта:**
```
src/product/
├── Product.java              ← Базовый класс (обновлен)
├── PhysicalProduct.java      ← Физические товары (обновлен)
├── DigitalProduct.java       ← Цифровые товары (обновлен)
├── pricing/
│   ├── PricePolicy.java      ← Интерфейс (НОВЫЙ)
│   ├── PercentageOff.java    ← Процентная скидка (НОВЫЙ)
│   ├── FixedOff.java         ← Фиксированная скидка (НОВЫЙ)
│   └── BogoHalf.java         ← Купи 1, получи 2-й за полцены (НОВЫЙ)
└── ShopDemo5.java            ← Демонстрация (НОВЫЙ)
```

**1. Интерфейс PricePolicy - основа полиморфизма**

[Открыть PricePolicy.java]

'Это сердце системы - интерфейс, определяющий контракт для всех ценовых политик:

```java
public interface PricePolicy {
    String name();                    // Название политики
    double apply(Product p, int qty); // Расчет финальной цены
    default boolean applicableTo(Product p) { return true; }
}
```

Ключевой момент: метод `apply()` НЕ изменяет товар - он ВЫЧИСЛЯЕТ и ВОЗВРАЩАЕТ новую цену. Это immutable подход.

**2. Три реализации интерфейса**

[Показать PercentageOff.java]

**PercentageOff** - процентная скидка от 0 до 90%:
```java
public double apply(Product p, int qty) {
    double unit = p.getPrice() * (1 - percent / 100.0);
    return unit * Math.max(0, qty);
}
```
Пример: товар 1000 KZT, скидка 10%, qty=2 → `1000 * 0.9 * 2 = 1800 KZT`

[Показать FixedOff.java]

**FixedOff** - фиксированная скидка с каждой единицы:
```java
public double apply(Product p, int qty) {
    double unit = Math.max(0.0, p.getPrice() - amount);
    return unit * Math.max(0, qty);
}
```
Пример: товар 1000 KZT, скидка 50 KZT, qty=2 → `(1000-50) * 2 = 1900 KZT`

[Показать BogoHalf.java]

**BogoHalf** - акция "купи один, второй за полцены":
```java
public double apply(Product p, int qty) {
    int pairs = Math.max(0, qty) / 2;
    int singles = Math.max(0, qty) % 2;
    return pairs * (price * 1.5) + singles * price;
}
```
Пример: товар 1000 KZT, qty=2 → `1 * 1500 = 1500 KZT` (экономия 500!)

Это демонстрирует **Interface-based polymorphism** - Product работает с интерфейсом PricePolicy, не зная о конкретных реализациях."

---

### ЧАСТЬ 3: ПОЛИМОРФИЗМ В КОДЕ (5 минут)

**[Открыть Product.java, показать строки 159-192]**

"Теперь давайте посмотрим на полиморфизм в классе Product.

**Compile-time Polymorphism: Method Overloading**

Я добавил ЧЕТЫРЕ перегруженных метода `finalPrice()` с разными сигнатурами:

```java
// 1) Цена за 1 единицу без скидок
public double finalPrice() {
    return getPrice();
}

// 2) Цена за qty единиц без скидок
public double finalPrice(int qty) {
    return getPrice() * qty;
}

// 3) Цена с одной политикой
public double finalPrice(int qty, PricePolicy policy) {
    if (policy == null || !policy.applicableTo(this))
        return finalPrice(qty);
    return policy.apply(this, qty);
}

// 4) Цена с выбором ЛУЧШЕЙ политики
public double finalPrice(int qty, List<PricePolicy> policies) {
    double best = Double.POSITIVE_INFINITY;
    for (var pp : policies) {
        double v = finalPrice(qty, pp);
        if (v < best) best = v;
    }
    return best;
}
```

Это **compile-time polymorphism** - компилятор выбирает нужный метод на этапе компиляции, анализируя количество и типы параметров.

Четвертый метод особенно интересен - он перебирает все политики, вызывает третий метод для каждой, и возвращает минимальную цену. Это демонстрирует reuse - переиспользование кода.

**Runtime Polymorphism: Method Overriding - PhysicalProduct**

[Открыть PhysicalProduct.java, строки 77-94]

Физические товары переопределяют методы, чтобы добавить стоимость доставки:

```java
@Override
public double finalPrice(int qty, PricePolicy policy) {
    double base = super.finalPrice(qty, policy); // Родительский метод
    return base + estimateShippingCost();        // + доставка
}
```

Логика:
1. Вызываем `super.finalPrice()` - получаем цену со скидкой
2. Добавляем `estimateShippingCost()` - доставка ОДИН РАЗ на заказ

Пример: Laptop 450,000 KZT, скидка 10%, доставка ~180 KZT
- Расчет: `450,000 * 0.9 + 180 = 405,180 KZT`

Важно: доставка добавляется к финальной цене после применения всех скидок, и только один раз на весь заказ, а не на каждый товар.

**Runtime Polymorphism: Method Overriding - DigitalProduct**

[Открыть DigitalProduct.java, строки 64-70]

Цифровые товары игнорируют акцию BogoHalf:

```java
@Override
public double finalPrice(int qty, PricePolicy policy) {
    if (policy instanceof BogoHalf) {
        return super.finalPrice(qty); // Игнорируем, возвращаем базовую цену
    }
    return super.finalPrice(qty, policy);
}
```

Почему? Потому что акция 'купи один, второй за полцены' не имеет смысла для цифровых товаров - у них нет себестоимости копирования. Это пример **type-specific behavior**.

Это демонстрирует **runtime polymorphism** - JVM выбирает метод во время выполнения в зависимости от фактического типа объекта."

---

### ЧАСТЬ 4: LIVE-ДЕМОНСТРАЦИЯ (3 минуты)

**[Открыть терминал]**

"Теперь давайте запустим программу и посмотрим, как это работает на практике.

[Выполнить компиляцию]

```bash
javac -d out src/category/*.java src/product/*.java src/product/pricing/*.java
```

Компиляция успешна - все файлы скомпилированы без ошибок.

[Запустить программу]

```bash
java -cp out product.ShopDemo5
```

**Анализ вывода:**

[Показать результаты для Laptop, qty=1]

'Смотрите на Laptop - физический товар за 450,000 KZT:

При qty=1:
- Base price: 450,000.00 KZT - базовая цена
- Percent-10%: 405,180.00 KZT - скидка 10% + доставка 180 KZT
- Fixed-50: 450,130.00 KZT - скидка 50 KZT + доставка
- BOGO-HALF: 450,180.00 KZT - нет пары, обычная цена + доставка
- Best: 405,180.00 KZT - выбрана лучшая

Обратите внимание: ко ВСЕМ ценам добавлена доставка ~180 KZT, потому что это PhysicalProduct.

[Показать результаты для Laptop, qty=2]

При qty=2 картина меняется:
- Base price: 900,000.00 KZT
- Percent-10%: 810,180.00 KZT
- Fixed-50: 900,080.00 KZT
- BOGO-HALF: 675,180.00 KZT - ВОТ ЭТО ИНТЕРЕСНО!
- Best: 675,180.00 KZT

BOGO-HALF стала САМОЙ выгодной! Почему?
- Расчет: 450,000 * 1.5 + 180 = 675,180 KZT
- Экономия: 900,180 - 675,180 = 225,000 KZT - четверть миллиона!

[Показать результаты для E-Book, qty=1 и qty=2]

Теперь E-Book - цифровой товар за 1,500 KZT:

При qty=1:
- Percent-10%: 1,350.00 KZT - лучшая
- Fixed-50: 1,450.00 KZT
- BOGO-HALF: 1,500.00 KZT - ИГНОРИРУЕТСЯ!

При qty=2:
- Percent-10%: 2,700.00 KZT - лучшая
- BOGO-HALF: 3,000.00 KZT - снова ИГНОРИРУЕТСЯ!

Видите? DigitalProduct игнорирует BogoHalf и возвращает базовую цену. Это работа переопределенного метода.

Также обратите внимание: НЕТ доставки для цифровых товаров.

**Что это демонстрирует:**

1. **Interface-based polymorphism**: один интерфейс PricePolicy, три разные реализации работают одинаково
2. **Method Overloading**: метод `finalPrice(qty, rules)` автоматически выбирает лучшую политику
3. **Method Overriding**: PhysicalProduct добавляет доставку, DigitalProduct игнорирует BogoHalf
4. **Polymorphic Collections**: `List<Product>` содержит разные типы товаров, `List<PricePolicy>` - разные политики"

---

### ЧАСТЬ 5: КЛЮЧЕВЫЕ ВЫВОДЫ (2 минуты)

"Давайте подведем итоги.

**Что было реализовано:**

✅ **Интерфейс PricePolicy** с тремя реализациями
   - PercentageOff (процентная скидка)
   - FixedOff (фиксированная скидка)
   - BogoHalf (купи один, второй за полцены)

✅ **4 перегруженных метода finalPrice()** в Product
   - Демонстрируют compile-time polymorphism
   - Предоставляют гибкий API для расчета цен

✅ **Переопределенные методы** в подклассах
   - PhysicalProduct добавляет доставку
   - DigitalProduct игнорирует BogoHalf
   - Демонстрируют runtime polymorphism

✅ **ShopDemo5** с полиморфными коллекциями
   - Единый код работает с разными типами

**Преимущества нового подхода:**

| Старый (applyDiscount) | Новый (PricePolicy) |
|------------------------|---------------------|
| Мутирует товар | НЕ мутирует (immutable) |
| Один тип скидки | Множество политик |
| Нельзя сравнить | Можно выбрать лучшую |
| Нельзя отменить | Можно пересчитать |
| Одинаково для всех | Каждый тип реагирует по-своему |

**Расширяемость:**

Чтобы добавить новую политику (например, "купи 3, получи 4-й бесплатно"), нужно:
1. Создать класс `BuyThreeGetOneFree implements PricePolicy`
2. Реализовать методы `name()` и `apply()`
3. Все! Не нужно менять Product или другие классы

Это следует принципу **Open/Closed Principle** (SOLID) - открыто для расширения, закрыто для модификации.

**Практическое применение:**

Эта архитектура используется в реальных e-commerce системах:
- Amazon - dynamic pricing с множеством правил
- Wildberries - акции, промокоды, скидки по категориям
- Kaspi.kz - кредитные программы, рассрочки

Спасибо за внимание! Готов ответить на вопросы."

---

## 🎯 ВОЗМОЖНЫЕ ВОПРОСЫ И ОТВЕТЫ

### Q1: "Почему вы выбрали интерфейс, а не абстрактный класс для PricePolicy?"

**Ответ:**
"Отличный вопрос! Я выбрал интерфейс по трем причинам:

1. **Множественная имплементация**: В Java класс может реализовать несколько интерфейсов, но наследовать только от одного класса. Если в будущем понадобится комбинировать политики, интерфейс даст больше гибкости.

2. **Контракт без состояния**: У PricePolicy нет общего состояния между реализациями - каждая политика независима. Интерфейс идеально подходит для определения контракта без реализации.

3. **Default методы**: Java 8+ позволяет интерфейсам иметь default методы. Я использовал это для `applicableTo()` - по умолчанию политика применима ко всем товарам, но можно переопределить.

Если бы у всех политик была общая логика (например, валидация параметров), тогда абстрактный класс был бы лучше."

---

### Q2: "Почему метод apply() не изменяет цену товара напрямую?"

**Ответ:**
"Это принципиальное решение, связанное с **immutability** (неизменяемостью):

**Проблемы мутирования:**
```java
// Плохо:
product.applyDiscount(10);  // Цена изменилась навсегда
product.applyDiscount(5);   // Скидка применяется к УЖЕ сниженной цене
// Как вернуть исходную цену? Сложно!
```

**Преимущества вычисления:**
```java
// Хорошо:
double price1 = product.finalPrice(qty, policy1); // Исходная цена не изменилась
double price2 = product.finalPrice(qty, policy2); // Можем сравнить
double price3 = product.finalPrice(qty, policy1); // Можем пересчитать
```

Это позволяет:
- Показать клиенту несколько вариантов цен
- Пересчитывать цену при изменении количества
- Избежать багов с повторным применением скидок
- Делать A/B тестирование разных политик

В реальных системах цены в базе данных остаются неизменными, а финальная цена вычисляется динамически."

---

### Q3: "Как метод finalPrice(qty, List<PricePolicy>) выбирает лучшую политику?"

**Ответ:**
"Давайте посмотрим на код:

```java
public double finalPrice(int qty, List<PricePolicy> policies) {
    double best = Double.POSITIVE_INFINITY; // Начинаем с бесконечности
    for (var pp : policies) {
        double v = finalPrice(qty, pp);     // Вызываем перегруженный метод
        if (v < best) best = v;             // Сохраняем минимум
    }
    return best;
}
```

Алгоритм:
1. Инициализируем `best` как бесконечность (самое большое число)
2. Для каждой политики вызываем `finalPrice(qty, pp)` - перегруженный метод с одной политикой
3. Сравниваем результат с текущим минимумом
4. Возвращаем наименьшую цену

Почему минимум? Потому что это выгоднее для покупателя. В реальных системах можно:
- Добавить параметр `boolean bestForCustomer` для выбора min/max
- Учитывать приоритет политик
- Комбинировать несколько политик (стратегия комбинирования)

Также обратите внимание: метод вызывает **сам себя** с другой сигнатурой - это пример переиспользования кода."

---

### Q4: "Почему доставка добавляется в PhysicalProduct, а не в Product?"

**Ответ:**
"Это демонстрация **принципа ответственности** (Single Responsibility) и **полиморфизма**:

**Причины:**
1. **Доставка специфична для физических товаров**: DigitalProduct не нужна доставка - это электронный файл
2. **Разная логика расчета**: PhysicalProduct считает доставку по весу и объему, у цифровых товаров этих параметров нет
3. **Демонстрация runtime polymorphism**: когда мы вызываем `product.finalPrice()` для `List<Product>`, JVM автоматически выбирает правильную версию метода

**Как это работает:**
```java
List<Product> items = List.of(laptop, ebook); // Physical и Digital

for (Product p : items) {
    p.finalPrice(qty, policy); // JVM решает какой метод вызвать:
    // Если p - PhysicalProduct → вызовется версия с доставкой
    // Если p - DigitalProduct → вызовется версия без доставки
}
```

Это **runtime polymorphism** в действии - одна и та же строка кода работает по-разному для разных типов."

---

### Q5: "Можно ли было реализовать это через Strategy Pattern?"

**Ответ:**
"Абсолютно! На самом деле, то что я реализовал - это **классический Strategy Pattern**:

**Компоненты паттерна в моем коде:**
- **Strategy** (интерфейс стратегии) = `PricePolicy`
- **ConcreteStrategy** (конкретные стратегии) = `PercentageOff`, `FixedOff`, `BogoHalf`
- **Context** (контекст) = `Product` с методом `finalPrice()`

**UML паттерна:**
```
┌──────────┐         ┌──────────────┐
│ Product  │────────▶│ PricePolicy  │
│ (Context)│         │ (Strategy)   │
└──────────┘         └──────────────┘
                            △
                            │
              ┌─────────────┼─────────────┐
              │             │             │
        PercentageOff   FixedOff     BogoHalf
      (ConcreteStrategy)
```

**Преимущества Strategy Pattern:**
- Инкапсулирует семейство алгоритмов (разные политики)
- Делает алгоритмы взаимозаменяемыми
- Позволяет выбирать алгоритм в runtime
- Следует Open/Closed Principle

Так что да, это Strategy Pattern + полиморфизм!"

---

### Q6: "Есть ли недостатки у вашего подхода?"

**Ответ:**
"Отличный критический вопрос! Да, есть компромиссы:

**1. Производительность:**
- Метод `finalPrice(qty, List<PricePolicy>)` вызывает `apply()` для каждой политики
- Для 10 политик это 10 вызовов
- Решение: кэширование результатов или ленивые вычисления

**2. Сложность API:**
- 4 перегруженных метода могут запутать начинающих
- Решение: документация и примеры использования

**3. Отсутствие комбинирования:**
- Сейчас можно применить ТОЛЬКО ОДНУ политику
- Нельзя сказать 'скидка 10% И еще 50 KZT'
- Решение: добавить `CompositePricePolicy` или метод `combine()`

**4. Нет приоритетов:**
- Все политики равны, выбирается минимум
- Что если есть эксклюзивные акции или промокоды?
- Решение: добавить `priority` или `exclusive` флаг

**Но для учебного проекта:**
Эти недостатки приемлемы, так как цель - демонстрация полиморфизма, а не production-ready система. В реальном проекте я бы добавил эти фичи."

---

## 📝 ЧЕКЛИСТ ПЕРЕД ПРЕЗЕНТАЦИЕЙ

Подготовка:
- [ ] Проект скомпилирован без ошибок
- [ ] ShopDemo5 запускается и выдает корректный вывод
- [ ] Открыты нужные файлы в IDE:
  - [ ] PricePolicy.java
  - [ ] BogoHalf.java
  - [ ] Product.java (строки 159-192)
  - [ ] PhysicalProduct.java (строки 77-94)
  - [ ] DigitalProduct.java (строки 64-70)
  - [ ] ShopDemo5.java
- [ ] Терминал готов для запуска команд
- [ ] Этот документ открыт для справки

Во время презентации:
- [ ] Говорить четко и уверенно
- [ ] Показывать код, объясняя каждую строку
- [ ] Акцентировать на ключевых моментах (immutability, polymorphism types)
- [ ] При демонстрации вывода - объяснять цифры
- [ ] Готовиться к вопросам, используя раздел "Возможные вопросы"

---

## 🎬 ПОРЯДОК ДЕМОНСТРАЦИИ КОДА

### 1. Показать интерфейс (30 сек)
```java
// PricePolicy.java
String name();
double apply(Product p, int qty);
default boolean applicableTo(Product p);
```

### 2. Показать одну реализацию детально (1 мин)
```java
// BogoHalf.java - самая интересная
int pairs = qty / 2;
int singles = qty % 2;
return pairs * (price * 1.5) + singles * price;
```

### 3. Показать перегрузку в Product (1 мин)
```java
// Product.java - все 4 метода
finalPrice()
finalPrice(int qty)
finalPrice(int qty, PricePolicy policy)
finalPrice(int qty, List<PricePolicy> policies)
```

### 4. Показать переопределение (1.5 мин)
```java
// PhysicalProduct.java
@Override
public double finalPrice(...) {
    double base = super.finalPrice(...);
    return base + estimateShippingCost();
}

// DigitalProduct.java
@Override
public double finalPrice(...) {
    if (policy instanceof BogoHalf) {
        return super.finalPrice(qty);
    }
    return super.finalPrice(qty, policy);
}
```

### 5. Запустить программу (2 мин)
```bash
javac -d out src/category/*.java src/product/*.java src/product/pricing/*.java
java -cp out product.ShopDemo5
```

### 6. Объяснить вывод (1 мин)
- Laptop: показать разницу при qty=1 и qty=2
- E-Book: показать, что BOGO игнорируется
- Подчеркнуть доставку для физических товаров

---

## 💡 ФИШКИ ДЛЯ ВПЕЧАТЛЕНИЯ

1. **Покажите расширяемость**: скажите "Чтобы добавить новую политику, нужно создать один класс, остальной код не меняется"

2. **Упомяните SOLID**: "Это следует Open/Closed Principle - открыто для расширения, закрыто для модификации"

3. **Реальные примеры**: "Такая же архитектура используется в Amazon, Wildberries, Kaspi.kz"

4. **Проблемы старого подхода**: четко объясните, почему applyDiscount() был плох

5. **Математика**: при демонстрации BOGO для qty=2, подсчитайте экономию: "225,000 KZT - почти четверть миллиона сэкономил покупатель!"

---

## ⚡ БЫСТРАЯ СПРАВКА: КЛЮЧЕВЫЕ ТЕРМИНЫ

- **Interface-based polymorphism**: PricePolicy с 3 реализациями
- **Compile-time polymorphism (Method Overloading)**: 4 метода finalPrice() с разными сигнатурами
- **Runtime polymorphism (Method Overriding)**: PhysicalProduct добавляет доставку, DigitalProduct игнорирует BogoHalf
- **Immutability**: apply() не изменяет товар, а вычисляет новую цену
- **Strategy Pattern**: семейство алгоритмов (политик), инкапсулированных и взаимозаменяемых
- **Open/Closed Principle**: открыто для расширения (новые политики), закрыто для модификации (Product не меняется)

---

**УДАЧИ НА ПРЕЗЕНТАЦИИ! 🚀**
